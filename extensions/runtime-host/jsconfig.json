{
	"//": [
		"The extension stays JavaScript: the web extension host loads it through",
		"new Function('module','exports','require', src), so there is no build step",
		"to compile a .ts file with. @ts-check plus JSDoc is the same checker.",
		"",
		"../types/vscode.d.ts is copied out of the vscode checkout by",
		"scripts/typecheck.mjs and is gitignored. Run that script once and the",
		"editor resolves require('vscode') too."
	],
	"compilerOptions": {
		"target": "es2022",
		"module": "commonjs",
		"lib": ["es2022", "dom", "webworker"],
		"checkJs": true,
		"strict": true,
		"noImplicitOverride": true,
		"noUnusedLocals": true,
		"noUnusedParameters": true,
		"noEmit": true,
		"types": []
	},
	"include": ["extension.js", "../types/vscode.d.ts"]
}
